import { AppTabDirective } from './app-tab.directive';

describe('AppTabDirective', () => {
  it('should create an instance', () => {
    const directive = new AppTabDirective();
    expect(directive).toBeTruthy();
  });
});
